#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#include "../include/utils.h"

void clearScreen() {
    system("cls"); // Use "cls" on Windows
}

void pauseScreen() {
    system("pause"); // Use "pause" on Windows
}